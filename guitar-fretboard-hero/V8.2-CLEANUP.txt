Guitar Fretboard Hero V8.2 — Dead Code Cleanup

Golden master: V8.1.
No intentional visual, layout, fretboard, control or gameplay changes.

Cleanup performed:
- Removed 68 obsolete/dead CSS selector occurrences left by historical UI generations.
- Removed 2 CSS rules whose selectors were entirely dead.
- Removed dead legacy selectors for old board IDs (#practiceBoard, #mapBoard, #quizBoard).
- Removed obsolete quiz drawer/backdrop selectors that have no DOM or JS implementation.
- Removed unused legacy selector families (attempt-pill, header-controls, pattern-controls,
  practice-meta, quiz-jump/quiz-jump-icon, section-title, shape-controls).
- Removed 7 CSS custom properties that have no var() consumer.
- Deduplicated repeated selectors inside rules while preserving cascade/order.
- app.js audit found no declared functions with zero call/reference sites; no JS removed.

Validation:
- app.js passes node --check.
- styles.css parses without top-level CSS parser errors.
- V8.1 HTML/JS/application structure and branding assets retained.
